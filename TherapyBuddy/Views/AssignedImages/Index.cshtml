@model IEnumerable<TherapyBuddy.Models.AssignedImage>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ExerciseImage.ImageURL)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ExerciseInstruction.Remark)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ExerciseImage.ImageURL)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ExerciseInstruction.Remark)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AssignedImageID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AssignedImageID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AssignedImageID })
        </td>
    </tr>
}

</table>
